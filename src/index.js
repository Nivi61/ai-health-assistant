import React from 'react';
import ReactDOM from 'react-dom/client';
import AIHealthAssistant from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <AIHealthAssistant />
  </React.StrictMode>
);